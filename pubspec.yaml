name: ngspice_dart
description: "A native Dart port of the ngspice mixed-signal circuit simulator: pure-Dart netlist parsing, MNA assembly, and OP/DC/transient/AC analyses with no native dependency."
version: 0.2.0
homepage: https://github.com/burhankhanzada/ngspice_dart

environment:
  sdk: ^3.5.0

# Pure Dart — no FFI, no native libraries, no Flutter requirement.
dependencies:

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
